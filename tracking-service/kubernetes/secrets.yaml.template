# This is a template file. Copy to secrets.yaml and fill in your actual values.
# DO NOT commit secrets.yaml to version control!
#
# To create the secret in Kubernetes:
# kubectl apply -f secrets.yaml

apiVersion: v1
kind: Secret
metadata:
  name: tracking-secrets
  namespace: bantam-shuttle
type: Opaque
stringData:
  db-username: "your-db-username"
  db-password: "your-db-password"
